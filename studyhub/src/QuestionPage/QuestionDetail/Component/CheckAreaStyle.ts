import { styled } from "@mui/material";
import CheckIcon from '@mui/icons-material/Check';

const CustomCheck = styled(CheckIcon) (
    () => ({
        color : "#42D948"
    })
)

export default CustomCheck;